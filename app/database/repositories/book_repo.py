"""
Book repository, data access layer.

Contains all SQL queries related to the 
'books' and 'books_collection' tables.
Returns Book model instances. No business logic,
just CRUD and search queries against PostgreSQL.
"""
