"""
User business logic.

Enforces rules like ownership checks and duplicate prevention.
Sits between routes and repositories, routes handle HTTP,
this module handles decisions, repos handle SQL.
"""
